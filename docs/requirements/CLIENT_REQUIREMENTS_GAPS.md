# Client Requirements — Gap & Conflict Register

Companion to `CLIENT_ACCEPTANCE_MATRIX.md`. This file lists every MISSING / INCORRECT / CONFLICT finding with severity + type classification, plus the full CONFLICT write-ups the audit task requires. Ranked most-severe first within each type. "Mandatory" = the customer Excel marks the row "Bắt buộc"; findings against non-mandatory ("Khuyến nghị"/"Normal"/"Tùy mục tiêu") rows are listed but do not drive the FAIL decision on their own.

Severity definitions used (per audit instructions): CRITICAL = core workflow broken / security violation / financial integrity issue / data isolation impossible / required workflow completely missing. HIGH = mandatory requirement materially incomplete / important role blocked from a required workflow / data model violates requirement / contract-payment-security issue. MEDIUM = important UX/workflow gap, partial requirement, operational limitation. LOW = wording/UI/detail mismatch.

**RE-AUDIT ROUND 2 (2026-08-25) note:** Full detail in `docs/requirements/CLIENT_ACCEPTANCE_REAUDIT_ROUND2.md`. Round 2 independently re-derived every "REMEDIATED" status below from the Excel wording rather than accepting the remediation report's own framing. Result: GAP-001/002/003 (Export, Payment-gate, Task.output) are genuinely and fully closed — confirmed RESOLVED. GAP-004/005/006/007 (Student fields, Commission traceability, Closure/Liquidation) are only **PARTIALLY REMEDIATED** — real, solid partial fixes, but each still has a concrete uncovered piece of the original mandatory requirement, detailed inline below. One **new HIGH finding** (GAP-022, `Student.school` field completely unimplemented) was found. Status tags below now use RESOLVED / PARTIALLY REMEDIATED / OPEN / DEFERRED / CONFLICT per the Round 2 instructions, replacing the old "REMEDIATED" label.

---

## CRITICAL findings

### GAP-001 — Export Control not implemented anywhere (mandatory, sheet07 row6 + sheet09 row8)
**STATUS: RESOLVED (2026-08-25, confirmed Round 2)** — `EXPORT_ROW_CAP=5000` + `enforceExportRowCap` applied to all 4 export endpoints (confirmed via a fresh repo-wide grep that exactly 4 export endpoints exist, all 4 capped, all 4 field-redacted); 409 `EXPORT_ROW_LIMIT_EXCEEDED` on overflow, never silent truncation. See docs/ASSUMPTIONS.md ASM-87 (the 5000 threshold is an engineering decision pending client confirmation, not a customer-specified number) and `CLIENT_ACCEPTANCE_MATRIX.md` REQ-AUDIT-005/REQ-SEC-007. Cosmetic-only residual: the code comment cites the wrong assumption ID (ASM-70 instead of ASM-87), zero functional impact.
**Type:** CODE BUG / SECURITY. **Requirement rows:** REQ-AUDIT-005, REQ-SEC-007.
Every export endpoint (`students.service.ts:127-131`, `reports.service.ts:150`, `payments.service.ts:271`, `contracts` export) runs an unbounded Prisma `findMany` with no `take`/`limit` and no row-count cap. A role with broad scope (GĐĐH, Trưởng phòng) can export its entire allowed dataset — every student, every contract, every payment — in a single unaudited-for-volume call. The action is logged (rowCount recorded after the fact), but nothing *prevents* the mass export the customer explicitly marked mandatory to block ("Không cho export hàng loạt", "Bắt buộc").
**Recommendation:** Add a hard server-side row-count cap to every export query (reject or paginate beyond N rows), or require a second-step approval for exports above a threshold.

### GAP-002 — Contract can activate (SIGNED→ACTIVE) with zero payment recorded (mandatory, sheet11)
**STATUS: RESOLVED (2026-08-25, confirmed Round 2)** — SIGNED→ACTIVE now requires at least one Payment PARTIALLY_PAID/PAID (409 `PAYMENT_REQUIRED_FOR_ACTIVATION`), race-safe via an interactive transaction + compare-and-swap (concurrency e2e test re-read line-by-line, confirmed it asserts the actual compare-and-swap outcome, not just a status code). **The exact threshold ("any payment" vs. a specific amount) was never customer-specified** — registered as CONFLICT-001, not silently assumed final. See docs/ASSUMPTIONS.md ASM-88. Round 2 note: the customer sheet names the activation actor as "Hệ thống" (System, automatic-on-payment); the implementation requires an explicit manual staff action instead — the gate itself is unaffected, logged as a LOW-severity automation-semantics note, not a defect.
**Type:** CODE BUG. **Requirement row:** REQ-CONTRACT-002.
`ContractsService.updateStatus` (contracts.service.ts:249-280) allows SIGNED→ACTIVE with no check against `Payment` records at all. The customer's 11-stage contract sequence places PAYMENT as its own stage between SIGNED and ACTIVE, implying activation should be payment-gated. As built, a contract becomes ACTIVE (triggering task generation, case linkage, service delivery) with the family having paid nothing.
**Recommendation:** Add a precondition to the ACTIVE transition requiring at least one non-pending `Payment` (or a specific minimum-paid threshold, to be defined with the client) before allowing activation — or obtain explicit written client sign-off that payment tracking is intentionally decoupled from the activation gate.

---

## HIGH findings

### GAP-003 — Task.output never enforced, contradicts explicit mandatory rule (sheet00 row9, sheet06)
**STATUS: RESOLVED (2026-08-25, confirmed Round 2)** — DONE transition now requires non-empty `output` (mirrors the BLOCKED/blocker precedent), 409 `OUTPUT_REQUIRED` otherwise. Bypass check: every code path that can set `Task.status='DONE'` (staff `updateStatus`, portal `portalUpdateStatus`) confirmed to funnel through the same `applyStatusTransition` gate — no parallel/looser path found. See docs/ASSUMPTIONS.md ASM-89.
**Type:** CODE BUG. **Requirement row:** REQ-TASK-004.
Customer states unconditionally: "Mọi công việc phải có Owner + Deadline + Output + Status." `Task.output` is nullable in the schema, absent from `CreateTaskDto` entirely, and optional even in `UpdateTaskStatusDto` when transitioning to DONE — with zero enforcement (contrast: the BLOCKED transition *does* require a non-empty `blocker`). A task can complete its full lifecycle with `output` permanently null.
**Recommendation:** Mirror the existing BLOCKED/blocker pattern — require non-empty `output` as a precondition for the DONE transition.

### GAP-004 — Student.school/grade/gpa/date_of_birth: required fields missing or optional (sheet04) — **RESOLVED 2026-08-25 (client decisions applied)**
**STATUS: RESOLVED (2026-08-25)** — ~~PARTIALLY REMEDIATED, stage-aware (confirmed Round 2 — `school` still fully missing, see new GAP-022)~~. All four sub-fields are now accounted for: `school` was confirmed present on `AcademicRecord` (GAP-022, corrected 2026-08-25 — was itself a Round 2 audit error); the stage-timing question (enforced at Assessment approval, not Student creation) is now **client-confirmed as the intended design** (2026-08-25), closing what had been the sole remaining open item for `grade`/`date_of_birth`; and `gpa` was resolved by direct client decision the same day as **Optional**, not mandatory (CONFLICT-004), which means it is no longer part of this requirement's "required fields" scope at all. **The one residual issue is that the code has not yet been updated to stop requiring `gpa` at the Assessment-approval gate — tracked separately as GAP-027 (MEDIUM), not as part of this HIGH finding**, since it's now a narrow code-cleanup item rather than a missing/unconfirmed requirement.
**Type:** DATA MODEL. **Requirement row:** REQ-STUDENT-002.
`date_of_birth` is optional in the schema; `grade`/`gpa`/`school` live on `AcademicRecord`. All four were marked "Bắt buộc" in the customer's Student Profile sheet; sheet17 internally disagreed with sheet04 on GPA's requiredness (CONFLICT-004), now resolved by client decision (Optional).
**Recommendation:** None remaining for this finding. See GAP-027 for the follow-up code change to `assertStudentProfileComplete`.

### GAP-005 — Student targeting fields (target_country/target_major/intake/scholarship_goal) not enforced or missing (sheet04) — **RESOLVED 2026-08-25 (stage-timing client-confirmed)**
**STATUS: RESOLVED (2026-08-25)** — ~~PARTIALLY REMEDIATED, stage-aware (confirmed Round 2)~~. `Student.scholarshipGoal` field exists; the Assessment-approval gate checks `targetCountry`/`targetMajor`/`targetIntake`/`scholarshipGoal`, confirmed no bypass path. The sole remaining open item — whether gating at Assessment-approval time rather than Student-creation time was the intended design — was **client-confirmed as intended on 2026-08-25**, closing this finding.
**Type:** DATA MODEL. **Requirement row:** REQ-STUDENT-004.
All four are "Bắt buộc" per the customer sheet and drive the entire downstream Roadmap/Scholarship/Application matching workflow. `targetCountry`/`targetMajor`/`targetIntake`/`scholarshipGoal` are enforced at Assessment approval, now confirmed as the client's intended enforcement point.
**Recommendation:** None remaining.

### GAP-006 — Contract↔Partner commercial link cannot be produced from the schema (sheet16) — **RESOLVED 2026-08-27 (DEC-09) — last remaining HIGH finding, now closed**
**STATUS:** ~~PARTIALLY REMEDIATED — HIGH severity confirmed, not a minor residual (Round 2)~~ — `CommissionTransaction.contractId` and `PartnerStudentLink.contractId`/`scholarshipApplicationId` added as real FKs. ~~Visa is still not directly joinable...~~
**2026-08-27 (DEC-09):** Client confirmed, verbatim: *"Có, bắt buộc liên kết trực tiếp Hoa hồng ↔ Visa đối với các khoản hoa hồng phát sinh từ Visa. Không bắt buộc đối với các khoản hoa hồng không có nguồn từ Visa. visaId là nullable ở cấp dữ liệu nhưng bắt buộc theo loại nguồn hoa hồng."* Implemented: `CommissionTransaction.visaId` (migration `20260827060023_commission_transaction_visa_link`), auto-resolved by `resolveSource()` when `sourceType==='Visa'` — the exact same mechanism already proven for `contractId`, so "required for Visa-sourced commissions, absent for others" falls out naturally with no separate conditional-validation layer. `sourceType` now accepts `'Contract' | 'Payment' | 'Visa'`. Since `Visa` carries no monetary field, `readSourceAmount()` explicitly rejects a non-FIXED `CommissionRule` on a Visa source (`COMMISSION_BASIS_MUST_BE_FIXED_FOR_VISA`) rather than silently misreading an unrelated Payment row. 5 new e2e tests; ~~full regression suite green.~~ **backend unit 209/209, backend e2e 556/557 (1 pre-existing, unrelated flake — see the DEC-16 verification note in `docs/DECISIONS.md`), frontend 325/325 — confirmed 2026-08-27. Live-browser-verified the same day: full FIXED-basis Visa-sourced commission lifecycle (create rule → create transaction → confirm eligible → calculate) exercised through the real UI end-to-end, `visaId` confirmed correct via direct DB query.** **Deliberately out of scope**: `PartnerStudentLink.visaId` — the client's answer was scoped to "Hoa hồng ↔ Visa" (Commission) specifically, not `PartnerStudentLink`; that remains a separate, still-open ask if wanted.
**Type:** DATA MODEL. **Requirement row:** REQ-PARTNER-008.
~~Neither CommissionTransaction nor PartnerStudentLink carries a contractId FK...~~ (historical — see REQ-PARTNER-008 in `CLIENT_ACCEPTANCE_MATRIX.md` for the full current state).
**Recommendation:** None remaining for `CommissionTransaction`. `PartnerStudentLink.visaId` remains available as a future, separate ask.

### GAP-007 — Closure/Liquidation: service-completion check exists but is unreachable by the Excel's designated role; document handover unchecked by any path (sheet11, sheet08 stage 14)
**STATUS: RESOLVED 2026-08-26** — the client answered the pending design decision (DEC-06/07/08, `CLIENT_CLARIFICATION_SIGNOFF.md`) and the unified Closure/Liquidation workflow was implemented. See `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md` and `docs/DECISIONS.md` DEC-13 for the full design; `CLIENT_ACCEPTANCE_MATRIX.md`'s "CLOSURE/LIQUIDATION REMEDIATION APPLIED (2026-08-26)" section for the verification record. Summary: one unified workflow (`ClosureService`), HCTH standard executor / ED-DM audited override / Consultant request-only (DEC-06); all 6 mandatory preconditions including a new, real Document Handover check, never auto-inferred (DEC-07); a structured, immutable two-party liquidation confirmation replacing the old free-text reason (DEC-08); Case/Contract states now synchronized in the same transaction; both old independent paths (`PATCH /cases/:id/close`, direct Contract COMPLETED/LIQUIDATED) retired.
**Type:** CODE BUG (frontend) + workflow gap + role-routing gap. **Requirement row:** REQ-CASE-014.
**Historical record (pre-remediation, kept for audit trail — no longer the current state):** ~~PARTIALLY REMEDIATED (revised twice in Round 2)~~ — Sheet11 row12 names 3 distinct COMPLETED-stage obligations: "dịch vụ hoàn thành, công nợ, tài liệu bàn giao" (service completion, debt, document handover), naming HCTH as the actor. Round 2 discovered a second, independent closure mechanism — `Case.close()` (`cases.service.ts:181-230`), gated to `cases:close` (EXECUTIVE_DIRECTOR/DEPARTMENT_MANAGER/CONSULTANT — **not** ADMIN_FINANCE/HCTH) — which DID check service completion (no open tasks, no outstanding debt, no in-progress Visa, confirmed Enrollment if an Application is in progress, a complete pre-departure checklist), while HCTH — the Excel's named closure actor — could only reach the weaker Contract-level path (debt only). Document handover was checked by neither mechanism. The two closure states (`Case.status`, `Contract.status`) were not synchronized. LIQUIDATED's "xác nhận hai bên" was a single-author free-text field; `Case.close()` had no liquidation concept at all.
**Recommendation:** ~~Design decision needed — see DEC-06/07/08.~~ **Implemented 2026-08-26 — no further action.**

### GAP-022 — Student.school ("Trường/Lớp") field completely unimplemented (sheet04 row5, mandatory) — RESOLVED 2026-08-25 (was itself a Round 2 audit error, not a code defect)
**STATUS: RESOLVED — this was Round 2's own mistake, corrected 2026-08-25 (not a code fix; nothing changed in `apps/api`/`apps/web`/`database`).**
**Type:** DATA MODEL. **Requirement row:** REQ-STUDENT-007.
Round 2's original claim ("no `school` column at all... genuinely absent") only checked `model Student` in `database/schema.prisma` and never checked `model AcademicRecord`, which has had a `school` field all along:
- `AcademicRecord.school String` — **NOT NULL** at the DB level (stricter than `grade`/`gpa`, both nullable by design).
- `CreateAcademicRecordDto.school` — `@IsString() @MinLength(1)`, no `@IsOptional()`: required on every create call.
- `academic-record-dialog.tsx` — a required form field.
- Indirectly guaranteed present by the existing `assertStudentProfileComplete` gate (it requires an `AcademicRecord` with `grade`+`gpa` set; no such record can exist without `school`, since the DTO rejects it).

"Trường/Lớp" is stored per-academic-period on `AcademicRecord` rather than as a single fixed value on `Student` — the same structural normalization already accepted for `grade`/`gpa` (see REQ-STUDENT-002's own note on that pattern), not a missing field.
**Recommendation:** None needed for the field itself. The remaining open question is the same stage-timing one already open for REQ-STUDENT-002/004 (creation-time vs. Assessment-approval-time enforcement) — no new recommendation beyond what's already tracked there.

---

## MEDIUM findings

### GAP-008 — Consultant has zero access to Partner Documents; sheet specifies "Hạn chế" (some access) (sheet03)
**Type:** CONFIGURATION. **Requirement row:** REQ-RBAC-006. Stricter-than-spec (low risk), but a real deviation.
**Recommendation:** Confirm with client whether zero access is acceptable, or add a scoped view-only grant.

### GAP-009 — HCTH under-provisioned on Partner Documents (view-only vs "Có") and Visa (zero vs "Hạn chế") (sheet03)
**Type:** CONFIGURATION. **Requirement row:** REQ-RBAC-007.
**Recommendation:** Add HCTH `partner_documents` create/edit; add a restricted HCTH `visa` view grant.

### GAP-020 — Sale/Marketing under-provisioned on Student Profile and Competition (zero vs "Hạn chế") (sheet03 rows 2, 6)
**Type:** CONFIGURATION. **Requirement row:** REQ-RBAC-012. Same "Hạn chế→zero" pattern as GAP-008/GAP-009, independently confirmed by both the backend RBAC pass (`seed.ts:581-605`, zero `students` grant) and the separate frontend RBAC pass (zero `profile_evidence` grant), which flagged it as a CONFLICT candidate before the two passes were cross-checked against each other.
**Recommendation:** Confirm with client whether zero access is acceptable for Sale/Marketing on Student Profile and Competition, or add a narrow read-only/summary-field grant on each.

### GAP-023 — HCTH (ADMIN_FINANCE) has zero access to Student Profile; sheet specifies "Hạn chế" (restricted, not zero) (sheet03 row2) — NEW, found in Re-Audit Round 2
**Type:** CONFIGURATION. **Requirement row:** REQ-RBAC-013 (new row).
`ADMIN_FINANCE` grant block (`database/seeds/seed.ts:613-641`) has no `students` resource grant of any kind — zero, not restricted. This is the 4th confirmed instance of the same "Hạn chế→zero" under-provisioning pattern already found for Consultant/Partner-Documents (GAP-008), HCTH/Partner-Documents+Visa (GAP-009), and Sale-Marketing/Student-Profile+Competition (GAP-020) — recurring across 3 different roles and 4 different module cells, which suggests a systematic issue in how the original RBAC seed was built from sheet03, not 4 unrelated oversights.
**Recommendation:** Confirm with client whether HCTH needs any Student Profile visibility (e.g. read-only name/contact, useful for correspondence about a contract) or zero is intentional. Given the pattern has now recurred 4 times, recommend a full cell-by-cell audit of `seed.ts` against sheet03 rather than patching instances one at a time.

### GAP-024 — `Case.stage` is unvalidated free text, not derived from actual sub-entity progress (sheet08) — **RESOLVED 2026-08-26**
**Type:** DATA MODEL / workflow integrity. **Requirement row:** REQ-CASE-016.
~~`Case.stage` (`cases.service.ts:128,143`, `update-case-stage.dto.ts`) is `@IsString() @MaxLength(100)` with no validated value set and no derivation from the ~10 real, independently-gated sub-entity statuses (Assessment/Roadmap/Application/Offer/Scholarship/Visa/Enrollment/Contract). Any staff member with `cases:edit` can set it to any string via `PATCH .../stage`. Each individual stage's own workflow gate is real and enforced (see REQ-CASE-001 through 015) — this is specifically about the summary label users see potentially drifting from what the underlying data shows, not about any individual stage being unenforced.~~
**Fixed 2026-08-26 (client-confirmed, DEC-14):** Client confirmed a controlled enum (not server-derivation from sub-entity status, which was a larger, uninvited change) is the intended fix. New `CaseStage` Prisma enum (15 values matching sheet08 exactly — `LEAD_TO_CONTRACT` ... `ARCHIVE`), `Case.stage String` → `Case.stage CaseStage`, DTOs changed to `@IsEnum(CaseStage)`, data-preserving migration (`20260826090333_case_stage_enum`), frontend renders the Vietnamese label and offers a dropdown of the 15 stages instead of free text. Backend unit 209/209, e2e full suite passing, frontend 325/325, live-browser-verified. See `docs/DECISIONS.md` DEC-14 and `CLIENT_ACCEPTANCE_MATRIX.md` REQ-CASE-016 for full detail.

### GAP-025 — `profile_evidence` RBAC resource is too coarse to express sheet03's per-module grant differences — NEW, found in Re-Audit Round 2 (root cause underlying GAP-020)
**Type:** CONFIGURATION / architecture. **Requirement row:** REQ-RBAC-012 (root-cause addition, not a new row).
`TestRecord`/`Competition`/`ResearchProject`/`Activity` are all gated by one shared `profile_evidence` resource (`profile-evidence.controller.ts`, every route). Sheet03 gives Sale/Marketing a *different* grant per module — "Không" on Luyện thi/NCKH/Resume-family but "Hạn chế" specifically on Competition — a distinction the current permission model cannot express even if the missing grant were added, because Competition and Luyện thi/NCKH share one gate.
**Recommendation:** Split `profile_evidence` into per-module RBAC resources, or get client agreement to a documented simplification (e.g., treat all four as one access tier).

### GAP-027 — `assertStudentProfileComplete` still hard-requires GPA, contradicting the client's confirmed Optional decision — **RESOLVED 2026-08-25**
**Type:** CODE BUG (behavior stricter than confirmed requirement). **Requirement row:** REQ-STUDENT-002, REQ-DATA-013.
The client resolved CONFLICT-004 on 2026-08-25: `Student.gpa` is **Optional**, not "Bắt buộc" (sheet17's reading, overriding sheet04). ~~But `AssessmentsService.assertStudentProfileComplete` was never updated to match — it still hard-blocks Assessment approval (409 `STUDENT_PROFILE_INCOMPLETE`) unless at least one `AcademicRecord` has **both** `grade` and `gpa` set.~~ **Fixed 2026-08-25:** the `gpa: { not: null }` clause was dropped from the `AcademicRecord` lookup in `assertStudentProfileComplete` — only `grade: { not: null }` remains. `assessment-roadmap.e2e-spec.ts` updated (error-message text, and a new test confirming approval succeeds with a graded-but-GPA-less record) — 20/20 passing. Two additional stale references to "GPA required" found during a fuller re-check and fixed in the same pass: the `academic-record-dialog.tsx` hint text, and the `STUDENT_PROFILE_INCOMPLETE` frontend error-message text in `error-messages.ts` (neither affected actual enforcement — both were descriptive UI copy only). `docs/ASSUMPTIONS.md` ASM-90 updated with a dated note.
**Recommendation:** None remaining.

### GAP-021 — Marketing has no standalone frontend module (sheet01 row15, sheet03 row16)
**Type:** CODE BUG (frontend) / possibly scope question. **Requirement row:** REQ-DEPT-002. No route, nav item, `lib/marketing/`, or `marketing` entry in the frontend's `Resource` permission union exists at all — Marketing-relevant data (lead source/campaign) is folded into the Lead entity instead of being a distinct module with its own per-role grant column as sheet03 describes.
**Recommendation:** Confirm with client whether "Marketing" was always meant to be Lead-source attributes (documentation/scoping fix only) or needs a real standalone module, page, and permission resource.

### GAP-010 — Partner.partner_type enum doesn't cover 6 of the 7 customer categories (sheet17)
**Type:** DATA MODEL. **Requirement row:** REQ-PARTNER-002. Actual enum: `UNIVERSITY_REPRESENTATIVE, AGENCY, LANGUAGE_CENTER, OTHER`. Required: University/School/Scholarship/Visa/Accommodation/Insurance/Recruitment.
**Recommendation:** Expand the `PartnerType` enum to match, or explicitly map and confirm with the client which categories collapse into "OTHER" today.

### GAP-011 — Partner Program ID generates with the wrong prefix (PT- instead of PP-) (sheet18) — **RESOLVED 2026-08-27**
**Type:** CODE BUG. **Requirement row:** REQ-PARTNER-005 / REQ-ID-004.
~~`nextPartnerProgramSuffix` concatenates the parent Partner's own `PT-CC-NNNNN` code plus a suffix, producing e.g. `PT-US-00001-01` instead of the documented `PP-US-00001-01`. Three separate doc files (`API_CONVENTIONS.md`, `DATA_DICTIONARY.md`, `ERD.md`) all describe the intended `PP-CC-NNNNN-NN` format — this is a genuine implementation bug against the project's own documentation, not an ambiguous spec.~~
**Fixed 2026-08-27:** `nextPartnerProgramSuffix` (`apps/api/src/common/id/id-generator.service.ts`) now substitutes `PT-` → `PP-` in the parent Partner's code before appending the `-NN` suffix — output is now `PP-CC-NNNNN-NN` as documented. `CC-NNNNN` and the per-partner sequence bucket are unchanged (identifier generation architecture reused, not replaced); no schema change; no existing `PartnerProgram` row rewritten. 5 new e2e regression tests cover prefix correctness, sequential numbering, DB uniqueness, immutability (client-supplied override rejected), and that the pre-existing fixture row is untouched. Full validation green: typecheck, lint (0 errors), backend unit 209/209, backend e2e 544/544.

### GAP-012 — Contract.contract_type field missing (sheet17)
**Type:** DATA MODEL. **Requirement row:** REQ-CONTRACT-008.
**Recommendation:** Add a `contractType` enum if the client needs to distinguish contract types beyond the existing free-text `servicePackage`.

### GAP-013 — Training (TRN) ID generation / entity entirely unimplemented (sheet18)
**Type:** CODE BUG / scope gap. **Requirement row:** REQ-ID-012. No `Training` model, service, or ID call-site exists anywhere in the backend (confirmed via full-repo grep). Note: Training is not listed among sheet01's 22 core modules, so this may be a lower-priority/future item rather than a core-workflow blocker.
**Recommendation:** Confirm with the client whether Training tracking is in scope for the current phase; if yes, build the model; if not, remove the TRN prefix from the ID rules sheet or mark it explicitly future-scope.

### GAP-014 — Encryption at rest: only the MFA secret has app-level encryption (sheet09 row13, mandatory)
**Type:** CONFIGURATION / possibly acceptable, needs client decision. **Requirement row:** REQ-SEC-012.
`Contract.value`, `Payment.amount`, `Student.budget` rely solely on the managed database's disk-level (platform) encryption, not application-level field encryption. This may fully satisfy a reasonable reading of "mã hóa dữ liệu nhạy cảm khi lưu," but the customer sheet doesn't specify which layer is acceptable.
**Recommendation:** Get an explicit client decision — platform-level encryption acceptable, or app-level field encryption required for these three fields.

### GAP-028 — Partner has no pre-onboarding "Prospect" lifecycle status (sheet13) — NEW, found 2026-08-27
**Type:** DATA MODEL. **Requirement row:** REQ-PARTNER-009 (new row).
Sheet13's own 7 example Partner rows are all seeded as `Prospect`, but `Partner.status` is `MasterDataStatus` = `ACTIVE | INACTIVE` only (schema.prisma:160-163) — there is no way to represent "identified but not yet vetted/onboarded." Every Partner must be immediately one of the two binary states.
**Recommendation:** Confirm with client whether Partner needs a real 3-state lifecycle (Prospect → Active → Inactive), or whether "Prospect" in sheet13 was just informal placeholder text in the client's own example data, not a required system state.

### GAP-029 — Contract has no direct link to PartnerProgram (sheet19 row14) — NEW, found 2026-08-27
**Type:** DATA MODEL / relationship. **Requirement row:** REQ-REL-006 (new row).
Sheet19 row14 describes `Contract N:1 PartnerProgram` ("Hợp đồng có thể gắn chương trình/đối tác liên quan"). No field exists in either direction — `Contract` has no `partnerProgramId`, `PartnerProgram` has no back-relation to `Contract`. The only real Contract-adjacent partner links are `PartnerStudentLink.contractId`/`CommissionTransaction.contractId` (junction entities pointing AT Contract from the Partner side), not the direct FK sheet19 describes. Same underlying gap family as GAP-006 (Application/Scholarship also lack a real `partnerProgramId`).
**Recommendation:** Likely resolved together once DEC-09 is answered — confirm with client whether Contract needs its own direct `partnerProgramId`, or whether the Application/Scholarship→PartnerProgram chain (once fixed) is sufficient without a redundant FK on Contract itself.

### GAP-030 — Contract Templates: 0 rows exist in the database (sheet12) — NEW, confirmed 2026-08-27
**Type:** DATA / seed gap, not a code defect. **Requirement row:** REQ-CONTRACT-007 (upgraded from NOT_TESTABLE to confirmed).
The mechanism (`ContractTemplate` model, merge-field auto-fill, create/list API) is real and works. Direct DB query against the local dev database confirms **`contract_templates` has 0 rows** — no seed script creates any of the 4 named templates (`HD-DUHỌC-01`, `HD-HB-01`, `HD-TD-01`, `HD-TOANDIEN-01`). Also newly noted: the model (`code`/`name`/`version`/`active`/`mergeFields`) has no field for sheet12's "Đối tượng" (audience), "Phạm vi" (scope), or "Có phụ lục" (has-appendix) columns.
**Recommendation:** Add a seed script creating the 4 templates with their sheet12 codes/names; confirm with client whether audience/scope/has-appendix need to be real, queryable fields or are documentation-only (informational text in the template content itself).

---

## LOW / informational findings (non-mandatory or cosmetic)

- **GAP-015** — ~~KPI reporting gaps (sheet06, all "Normal" priority, not mandatory): case-count-per-staff, task/case ratio, essay/resume/SOP/LOR completion counts, on-time-closure rate, "internal profile error" count, and customer-response-time SLA are all MISSING from the reporting layer. The last two ("internal profile error," "response time SLA") have no defined measurement points anywhere in the system and need a scoping conversation with the client before they can even be designed, not just built.~~ **PARTIALLY RESOLVED 2026-08-26** — re-checked directly against the source XLSX (sheet06 "Task_KPI", rows R3-R19) rather than a re-read of this prior note. 6 of the 9 originally-listed items are now built (case count per owner, task/case ratio, Essay/Resume/SOP/LOR completion — merged into one "Writing hoàn thành" count per client decision, since `WritingArtifact.type` has no controlled vocabulary — pre-departure checklist completion): see `CLIENT_ACCEPTANCE_MATRIX.md` REQ-KPI-001/002/005/006. One further item, **profile-completeness % (sheet06 row6, "Hồ sơ hoàn chỉnh")**, was found not to have been logged as its own requirement at all in the original pass — now REQ-KPI-010, also built 2026-08-26. **Still genuinely open, client-confirmed 2026-08-26 to skip rather than have the team invent a definition:**
  - **On-time contract-closure rate** (sheet06 row16, REQ-KPI-007) — no target-closure-date field exists anywhere in the schema to measure "on time" against. Needs the client to define the target/deadline concept (a new field on Case/Contract, or reuse of an existing Task deadline) before this can be built.
  - **Internal profile error count** (sheet06 row18, REQ-KPI-008) — no "error" concept exists anywhere in the data model. Needs the client to define what counts as an internal error operationally (e.g. rejected/returned documents, tasks reopened after DONE, a formal complaint record) before this can be designed, not just built.
  - **Customer response-time SLA** (sheet06 row19, REQ-KPI-009) — no inbound-contact/first-response timestamp exists anywhere. Needs the client to define what starts the clock (Lead creation? first outbound contact logged?) and what stops it (first Task created? a logged reply?) before this can be built.
  **Type:** DATA MODEL / scope-undefined (for the 3 remaining items only).
- **GAP-016** — Frontend role-nav is a static hand-authored mirror of the backend RBAC seed (`ROLE_GRANTS` in `rbac-data.ts`), explicitly commented as "UX ONLY... every grant re-checked server-side." Not a security hole, but a real drift-risk if the backend seed changes without a matching frontend update. **Type:** ENVIRONMENT / maintenance risk.
- **GAP-017** — `AuditLog.action`, `Document.document_type`, and `Task.output`'s sibling `Task.status`-adjacent fields use free-text `String` where the customer dictionary specifies an Enum (action, document_type). Low risk since these appear to be validated at the application layer even without a DB-level enum, but worth tightening. **Type:** DATA MODEL.
- **GAP-018** — List/browse endpoints (`GET /students`, `GET /contracts`, etc.) are not `@Audit`-decorated — only single-record VIEW is captured. Sheet07's own example row only shows single-record VIEW, so this may be intentional; flagging for client confirmation rather than as a confirmed defect. **Type:** CONFIGURATION, ambiguous severity.
- **GAP-019** — ~~`PartnersService.getById` takes no `principal` and performs no record-level scope check (relies only on the role-permission gate) — inconsistent with Student/Contract's pattern. Low risk since Partner data appears intentionally GLOBAL-readable by every granted role by design, but the inconsistency itself (vs. the project's own stated "no ID-based authorization" rule) is worth a deliberate decision rather than an accidental one. **Type:** CODE BUG (architecture inconsistency), severity depends on client's answer to GAP-020.~~ **RESOLVED 2026-08-27 (DEC-03) — not a gap.** Client confirmed: Partner is shared company-wide data, not case-scoped; any staff with `partners:view` is meant to see every Partner record company-wide. Sensitive Partner sub-data/actions stay controlled via permission and field-level access (unchanged). No code change needed — `PartnersService.getById`'s lack of a record-level scope check is the client's intended design, not an inconsistency. See `CLIENT_CLARIFICATION_SIGNOFF.md` DEC-03 and `CLIENT_ACCEPTANCE_MATRIX.md` REQ-RBAC-011/REQ-SEC-017/REQ-PARTNER-006. **Type:** CODE BUG (architecture inconsistency) — reclassified NOT A GAP.
- **The project's local `.env` currently points `DATABASE_URL` at the production Supabase database**, not the local Docker Postgres — discovered incidentally while attempting to set up an isolated UAT test environment for this audit (see `CLIENT_UAT_SCENARIOS.md`). Not a customer-requirements gap, but an operational-hygiene risk worth fixing: anyone running a local migrate/seed command without checking `.env` first could accidentally target production. **Type:** ENVIRONMENT.
- **GAP-026** — ~~`Activity` model (sheet04 rows 9-13, "Hoạt động ngoại khóa/Leadership/Community," priority "Tùy mục tiêu" — not mandatory) has no `award` field~~ **RESOLVED 2026-08-25.** `Activity.award String?` added (migration `20260825084819_activity_award_field`), matching `Competition.award`/`ResearchProject.award`. Wired through `CreateActivityDto` (backend), `Activity`/`CreateActivityInput` types and `activity-dialog.tsx` (frontend form, "Giải thưởng" field), and displayed in the Activity list on the Case profile page. Backend/frontend typecheck clean; `profile-evidence.e2e-spec.ts` (17/17) and the frontend profile page test (5/5) re-verified passing. **Type:** DATA MODEL, non-mandatory. **Recommendation:** None remaining.

---

## CONFLICT register (customer requirement vs. implementation, ambiguous or genuinely divergent — requires client decision, not silent resolution)

### CONFLICT-001 — Contract status sequence (11 stages vs. 9 implemented; PAYMENT and AMENDED not modeled as statuses)
Already fully detailed in `CLIENT_ACCEPTANCE_MATRIX.md` under REQ-CONTRACT-001. Summary: customer's literal 11-value sequence includes PAYMENT and AMENDED as Contract-level statuses; implementation models Payment as a separate child entity and Amendment as a separate audit-trail entity + version counter, neither ever appearing in `Contract.status`. GAP-002 above is the concrete functional consequence (no payment gate). **Customer acceptance required: YES.**

### CONFLICT-002 — SYSTEM_ADMIN role not named in customer sheets 02/03
**Customer says:** 7 roles total, exhaustively listed in sheet02 (GĐĐH, Trưởng phòng, Tư vấn, Hồ sơ, Sale/Marketing, HCTH, HS/PHHS).
**Current implementation:** An 8th `RoleCode`, `SYSTEM_ADMIN`, exists — narrowly scoped to `users`/`audit_logs`/`jobs` administration, zero business-data grants.
**Difference:** A platform/IT-operations role outside the customer's business-role taxonomy.
**Current DECISION/ASSUMPTION:** None found in `docs/DECISIONS.md`/`docs/ASSUMPTIONS.md` addressing this specific role's existence against the customer's exhaustive 7-role list.
**Impact:** Low — the role carries zero business-data exposure and satisfies the customer's own Offboarding/Session-Control requirements (sheet09), which need *some* identity-administration actor. But it was never reviewed against the customer's explicit, closed role list.
**Recommendation:** Formally document SYSTEM_ADMIN's existence and scope for client sign-off; no code change needed unless the client objects.
**Customer acceptance required: YES** (a documentation/sign-off gap, not a functional one).

### CONFLICT-003 — "Đối tác chỉ xem dữ liệu được chia sẻ theo từng case" (sheet09 row18) — ambiguous scope
**Customer says:** Partner access is restricted to data shared per-case.
**Current implementation:** No external partner-facing login/portal exists anywhere — Partner is purely an internal-staff-managed CRM record set. Internally, `PartnersService.list/getById` apply **no case-ownership filter at all** — every role holding any `partners:view` grant sees every Partner row globally.
**Difference:** The requirement is genuinely ambiguous between two readings: (a) an *external* partner-login control (in which case it's NOT_APPLICABLE — no such login surface exists to scope), or (b) an *internal staff* case-scoping rule (in which case it's a real, unaddressed gap — internal roles currently see all partners regardless of case relevance).
**Current DECISION/ASSUMPTION:** None found addressing which reading was intended.
**Impact:** Low if reading (a) was intended (nothing to fix); Medium if reading (b) was intended (a real internal over-exposure, though bounded by role-permission gating, not a raw data leak to unauthorized roles).
**Recommendation:** Ask the client directly which reading was intended; if (b), add a case-scope filter to Partner list/read queries analogous to `ScopePolicyService`'s existing Student/Contract/Case filters.
**Customer acceptance required: YES.**

### CONFLICT-004 — Student.gpa requiredness: sheet04 vs. sheet17 disagree (found in Re-Audit Round 2) — **RESOLVED 2026-08-25 (client decision: Optional)**
Full detail in `docs/requirements/CLIENT_REQUIREMENT_CONFLICTS.md`. Summary: sheet04 row6 marks GPA "Bắt buộc"; sheet17 row7 marks the same field "Optional" — the customer's own two source sheets disagreed with each other. ~~The current implementation happens to follow sheet04 (enforced at Assessment approval), but this was not a deliberate documented tie-break.~~ **Client decision (2026-08-25): GPA is Optional** — sheet17's reading wins. This exposed that the implementation's stricter, undocumented tie-break (following sheet04) is now out of step with the confirmed requirement — see new finding **GAP-027** above. **Customer acceptance obtained: YES (2026-08-25).**

---

## Summary tally — RE-AUDIT ROUND 2 (2026-08-25; see `CLIENT_ACCEPTANCE_REAUDIT_ROUND2.md` for full methodology)

- CRITICAL: 0 (GAP-001, GAP-002 — both independently confirmed RESOLVED)
- HIGH: 5 total identified this audit (registry count, includes resolved items — unchanged since GAP-004/005 resolving doesn't remove a real finding from the historical count, same as GAP-003) — **GAP-003, GAP-004, GAP-005 RESOLVED**; **GAP-007 RESOLVED 2026-08-26** (see its entry above — unified Closure/Liquidation workflow implemented per DEC-06/07/08); **GAP-006 remains PARTIALLY REMEDIATED** (Commission↔Visa traceability — separate, unrelated open item, explicitly out of scope for the Closure remediation); ~~GAP-022 (NEW) — `Student.school` field completely unimplemented, OPEN~~ **GAP-022 RESOLVED 2026-08-25 — this was Round 2's own audit error, not a code defect (excluded from the registry count itself, unlike the other 5, since it was never a real finding); see the corrected entry above and `CLIENT_ACCEPTANCE_MATRIX.md`'s "POST-ROUND-2 CORRECTION" section.** **Currently open/unresolved at HIGH severity: 1 (GAP-006).**
- MEDIUM: 13 total identified (registry count, includes resolved items, same convention as HIGH above) — GAP-008 through GAP-014, GAP-020, GAP-021 unchanged, code not touched by remediation, re-verified via git diff against the pre-remediation baseline; GAP-023, GAP-024, GAP-025 (Round 2, still open); **GAP-027 RESOLVED 2026-08-25** — code fixed, see its entry above. **Currently open/unresolved at MEDIUM severity: 12** (13 minus GAP-027).
- LOW/informational: 7 total identified (registry count) — GAP-015 through GAP-019, plus the `.env` note, unchanged; **GAP-026 RESOLVED 2026-08-25** — `Activity.award` field added, see its entry above. **Currently open/unresolved at LOW severity: 6** (7 minus GAP-026).
- CONFLICT requiring explicit customer decision: 4, of which 1 now resolved (CONFLICT-001/002/003 unchanged and still fully open; **CONFLICT-004 RESOLVED 2026-08-25** — client decided GPA is Optional, see GAP-027 above for the follow-up code mismatch this exposed)

**Round 2 revision note (superseded in part by the 2026-08-25 client decisions below):** Of the 4 previously-HIGH findings addressed by remediation (GAP-003 through 007 excluding the 2 CRITICAL), Round 2 judged only GAP-003 (Task.output) genuinely fully closed, with GAP-004/005/006/007 all left partial. GAP-004/005 (Student fields) were solid for 7 of 8 named `Student`-table fields at the time, with `school` (Round 2 believed "missed," later corrected — see GAP-022) and the stage-timing question both cited as the reasons for the PARTIAL label. **As of 2026-08-25, both reasons are resolved — GAP-022 was an audit error (school already existed) and the stage-timing question is now client-confirmed as intended — so GAP-004 and GAP-005 are now marked RESOLVED (see their entries above), not partial.** GAP-006 (Commission traceability) fixed 2 of 3 named link legs (Contract, Scholarship) but not Visa — still open. GAP-007 (Closure/Liquidation) built a real, reachable, debt-checked UI, and a second closure path (`Case.close()`) turned out to already enforce service-completion — but that path is unreachable by HCTH, the role the Excel names for closure; document handover is checked by neither path; two-party liquidation confirmation remains unenforced free text — still open. None of these are regressions or fabricated fixes — each fix (including the now-resolved GAP-004/005) is real, tested, and either fully closes or genuinely improves on the pre-remediation state.

**Post-Round-2 correction note (2026-08-25):** While checking sheet04 (`04_Student_Profile`) row-by-row against the implementation, GAP-022 was found to be a false positive from Round 2's own methodology: Round 2 only searched `model Student` in `database/schema.prisma` for a `school` field and, finding none, concluded the field was "genuinely absent." It did not check `model AcademicRecord`, where `school String` (NOT NULL) has existed all along — required at the DTO layer (`create-academic-record.dto.ts`) and the UI form layer (`academic-record-dialog.tsx`), and indirectly guaranteed present on any qualifying record by `assessments.service.ts`'s `assertStudentProfileComplete` gate. This mirrors the already-accepted structural-normalization pattern used for `grade`/`gpa` (also on `AcademicRecord`, not `Student`) and for `TestRecord` vs. a flat `Student.language_scores` field. No code changed — this is a documentation correction to Round 2's own output, tracked as GAP-022 RESOLVED rather than removed, per this project's "never silently rewrite, always append a dated correction" convention. Full detail: `CLIENT_ACCEPTANCE_MATRIX.md`'s "POST-ROUND-2 CORRECTION (2026-08-25)" section and the corrected GAP-022 entry above. The same pass surfaced one genuinely new, minor, non-mandatory finding: GAP-026 (`Activity` model has no `award` field, unlike `Competition`/`ResearchProject`).

**Client decisions applied (2026-08-25):** The client directly answered three open questions this session. (1) **Stage-timing** — gating Student mandatory fields at Assessment-approval time (not creation time) is confirmed as the intended design; this closes the recurring open item behind REQ-STUDENT-002/004/007 and `CLIENT_CLARIFICATION_SIGNOFF.md` DEC-05(b) — no code change needed. This single decision is what allows GAP-004 and GAP-005 above to move from PARTIALLY REMEDIATED to RESOLVED. (2) **GPA requiredness (CONFLICT-004)** — resolved as Optional (see the corrected CONFLICT-004 entry above); this decision exposed GAP-027 (`assertStudentProfileComplete` still requiring GPA). (3) **Awards for pure extracurricular activities** (GAP-026/REQ-STUDENT-008) — client confirmed the field should be added.

**Code fixes applied (2026-08-25):** Following the three decisions above, the client asked for the corresponding code changes. **GAP-027 fixed**: `assertStudentProfileComplete` no longer requires `gpa`, only `grade`. **GAP-026 fixed**: `Activity.award String?` added (migration `20260825084819_activity_award_field`), wired through backend DTO, frontend types/form/display. A follow-up full re-check of every sheet04 field against current code (all 18 rows) found no further Student Profile gaps, and surfaced two stale UI-copy references to "GPA required" (a hint string in `academic-record-dialog.tsx`, and the `STUDENT_PROFILE_INCOMPLETE` message in `error-messages.ts`) — both fixed in the same pass; neither had ever affected actual enforcement. Backend/frontend typecheck clean, lint clean; `assessment-roadmap.e2e-spec.ts` (20/20), `profile-evidence.e2e-spec.ts` (17/17), and the frontend profile page test (5/5) all re-verified passing after the changes. See REQ-STUDENT-002/008 in `CLIENT_ACCEPTANCE_MATRIX.md` for the corresponding requirement-status updates.

**Client decisions applied + code fixes (2026-08-26):** The client answered the pending Closure/Liquidation design decision (DEC-06/07/08, `CLIENT_CLARIFICATION_SIGNOFF.md`) and asked for the corresponding implementation. **GAP-007 fixed**: see its entry above for the full summary; full design record in `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md`, architecture decision in `docs/DECISIONS.md` DEC-13. Backend/frontend typecheck and lint clean; 23 new backend unit tests, 16 new backend e2e tests (plus updates to 4 existing e2e files), 6 new frontend tests, all passing; full regression 209 unit / 534 of 535 e2e (1 pre-existing, unrelated flake — see the design doc) and 317/317 frontend; production build clean for both apps.

**Journeys/UX correction (Round 2):** Journey I (Parent Portal / linked-child) was carried forward from Round 1 as IMPLEMENTED without the code ever having been opened. Round 2 opened it — `portal-access.service.ts:160-165` genuinely, server-side resolves every linked student via the real `StudentContact` table. Confirmed IMPLEMENTED, upgraded from an unverified to a verified claim, not a new gap.
