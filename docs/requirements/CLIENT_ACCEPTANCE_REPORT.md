# Client Acceptance Report — Abroad Scholarship System

**Audit type:** Client Acceptance / SRS Conformance Audit (read-only — no code, schema, or data changes made).
**Source of truth:** `docs/He_thong_quan_ly_du_hoc_hoc_bong.xlsx` (all 21 sheets read in full).
**Method:** Static code review (backend `apps/api`, frontend `apps/web`, `database/schema.prisma`) cross-referenced against every sheet, using direct file citation. No production data was read, modified, or fabricated. See `CLIENT_UAT_SCENARIOS.md` for why live execution was not possible this pass.
**Companion documents:** `CLIENT_ACCEPTANCE_MATRIX.md` (131 requirement rows + a 9-scenario Customer Journey section, full citations, Round 2 changelog at the top, plus a POST-ROUND-2 CORRECTION section), `CLIENT_REQUIREMENTS_GAPS.md` (26 gaps + 4 formal conflicts, Round 2 tally), `CLIENT_REQUIREMENT_CONFLICTS.md` (4 conflicts, full write-ups), `CLIENT_ACCEPTANCE_REAUDIT_ROUND2.md` (full Round 2 methodology and findings), `CLIENT_UAT_SCENARIOS.md` (original 9 journeys + role UAT, code-inferred; superseded for journey verdicts by the Round 2 re-derivation above).

**POST-ROUND-2 CORRECTION (2026-08-25):** Round 2's `GAP-022`/`REQ-STUDENT-007` finding ("`Student.school` completely unimplemented") was itself an audit error, not a code defect — the field exists on `AcademicRecord` (NOT NULL, required at DTO and form level) and Round 2's search only checked `model Student`. This correction reduces the HIGH-finding count and the mandatory-failure count below by 1, and moves REQ-STUDENT-007 from MISSING to IMPLEMENTED. A new, unrelated, low-severity finding (GAP-026 — `Activity` model missing an `award` field) surfaced during the same check and is tracked as a new row, REQ-STUDENT-008. **All totals and narrative in this report below have been updated to reflect the correction; nothing was silently rewritten — full detail in `CLIENT_ACCEPTANCE_MATRIX.md`'s "POST-ROUND-2 CORRECTION" section and the corrected `GAP-022`/new `GAP-026` entries in `CLIENT_REQUIREMENTS_GAPS.md`.**

---

## Methodology note on the numbers below

126 requirement rows were extracted and evaluated, **excluding** the `REQ-DATA-*` (12 rows) and `REQ-REL-*` (5 rows) blocks in the matrix — those two blocks re-examine the *same* underlying fields and relationships already scored under `REQ-STUDENT-*`, `REQ-CONTRACT-*`, `REQ-TASK-*`, `REQ-AUDIT-*`, and `REQ-PARTNER-*`. Counting them again would double-penalize (or double-credit) the same facts. They remain in the matrix as a structural cross-check (the customer's Data Dictionary and Relationship-diagram sheets deserve their own explicit pass) but are excluded from this summary's totals for that reason. This is a scoping decision made transparently, not a way to hide findings — every fact they contain is still counted once, under its functional requirement.

Two pairs of INCORRECT findings share one root cause each (Export Control appears in both sheet07 and sheet09; the Partner Program ID bug appears in both sheet18's ID list and the Partner module review) — the Gaps register counts these as one gap each (GAP-001, GAP-011) even though the matrix correctly lists both source rows.

---

## CUSTOMER REQUIREMENTS: 131 (127 + 3 new rows found in Re-Audit Round 2: REQ-STUDENT-007, REQ-RBAC-013, REQ-CASE-016 + 1 new row found in the 2026-08-25 post-Round-2 correction pass: REQ-STUDENT-008)

## IMPLEMENTED: ~~95~~ ~~97 (2026-08-27 — REQ-SEC-017/REQ-PARTNER-006 CONFLICT→IMPLEMENTED and REQ-RBAC-011 PARTIAL→IMPLEMENTED, all confirmed by client design decision DEC-03, no code change)~~ **98 (2026-08-27, later same day — REQ-PARTNER-008 PARTIAL→IMPLEMENTED via DEC-09, the Commission↔Visa link; see the full row in `CLIENT_ACCEPTANCE_MATRIX.md`)** (was 91 — REQ-STUDENT-007 corrected MISSING→IMPLEMENTED; REQ-STUDENT-004 upgraded PARTIAL→IMPLEMENTED once stage-timing was confirmed intended; REQ-STUDENT-002 and REQ-STUDENT-008 upgraded PARTIAL→IMPLEMENTED once GAP-027/GAP-026 were fixed in code — all 2026-08-25)
## PARTIAL: ~~18~~ ~~17 (2026-08-27 — REQ-RBAC-011 upgraded to IMPLEMENTED via DEC-03)~~ **16 (2026-08-27, later same day — REQ-PARTNER-008 upgraded to IMPLEMENTED via DEC-09)** (was 20 — new row REQ-STUDENT-008 added as PARTIAL then upgraded to IMPLEMENTED same day; REQ-STUDENT-002 also upgraded out of PARTIAL to IMPLEMENTED)
## MISSING: 8 (was 9 — REQ-STUDENT-007 corrected to IMPLEMENTED, 2026-08-25; see note below)
## INCORRECT: 6
## NOT_TESTABLE: 1
## CONFLICT: ~~3~~ **2 (2026-08-27 — REQ-SEC-017/REQ-PARTNER-006 resolved IMPLEMENTED via DEC-03, no longer a CONFLICT row)**
## NOT_APPLICABLE: 0

**Correction note (2026-08-25):** These totals were revised from Round 2's own reported figures (130 requirements; 91 IMPLEMENTED, 20 PARTIAL, 9 MISSING) in two passes. First, a follow-up check of Excel sheet04 against the Student-profile implementation found that Round 2's `REQ-STUDENT-007`/`GAP-022` finding — "`Student.school` completely unimplemented" — was itself an audit error: the field exists on `AcademicRecord` (NOT NULL, DTO- and form-required), and Round 2's search never checked that model. The same check added one new row, `REQ-STUDENT-008` (sheet04 rows 9-13, NCKH/Competition/Leadership/Community/Awards), scored PARTIAL for a genuinely new, minor, non-mandatory finding (`Activity` has no `award` field — GAP-026). Second, the client then directly confirmed (2026-08-25) that the stage-timing question underlying `REQ-STUDENT-002/004/007`'s PARTIAL status — whether gating mandatory Student fields at Assessment-approval time, not creation time, was intended — is indeed the intended design; this upgraded `REQ-STUDENT-004` from PARTIAL to IMPLEMENTED. The client separately resolved CONFLICT-004 (GPA required vs. optional) as **Optional**, which exposed a new, narrow code/requirement mismatch (GAP-027 — see `CLIENT_REQUIREMENTS_GAPS.md`) but did not itself change any row's Status in this matrix. No code was changed in the course of any of these corrections. Full detail: `CLIENT_ACCEPTANCE_MATRIX.md`'s "POST-ROUND-2 CORRECTION" and "CLIENT DECISIONS APPLIED" sections.

**Accounting note (Round 2):** CONFLICT-004 (the sheet04-vs-sheet17 GPA contradiction, `REQ-DATA-013`) is tracked in full in `CLIENT_REQUIREMENT_CONFLICTS.md` and `CLIENT_REQUIREMENTS_GAPS.md` (4 conflicts total there), but is **not** double-counted in the CONFLICT figure above — it lives in the `REQ-DATA-*` block, which this report's own stated methodology (above) excludes from the requirement-count totals as a re-examination of an already-scored field. ~~The 3 CONFLICT rows counted here are the three REQ-level conflicts: REQ-RBAC-002 (SYSTEM_ADMIN), REQ-CONTRACT-001 (payment sequence), REQ-SEC-017/REQ-PARTNER-006 (partner scope, one shared root cause).~~ **2026-08-27: REQ-SEC-017/REQ-PARTNER-006 moved to IMPLEMENTED (DEC-03 — client confirmed no case-scoping needed). The 2 CONFLICT rows now counted here are REQ-RBAC-002 (SYSTEM_ADMIN) and REQ-CONTRACT-001 (payment sequence — only its literal PAYMENT/AMENDED-as-enum-value question remains open; the threshold question is resolved via DEC-01).**

**COVERAGE (IMPLEMENTED / applicable):** ~~95 / 130 ≈ 73%~~ **98 / 130 (excluding the 1 NOT_TESTABLE row) ≈ 75% fully conformant (2026-08-27, current)** (was 71% before this session's corrections and fixes). Including PARTIAL as half-credit: (98 + 16×0.5) / 130 ≈ **82%**.

## MANDATORY REQUIREMENTS: 114 (of 131; 17 rows are Khuyến nghị/Normal/Tùy mục tiêu and excluded from this count — includes the new REQ-STUDENT-008 row, priority "Tùy mục tiêu")
## MANDATORY FAILURES: ~~5~~ **4 unique root-cause defects remaining (2026-08-27 — GAP-006 resolved via DEC-09, 5 − 1 = 4)** (was 9 originally, then 8 after removing the phantom GAP-022, then 7 after resolving GAP-004/GAP-005 and adding the newly-exposed GAP-027, then 6 after GAP-027 was fixed in code (2026-08-25); GAP-007 resolved 2026-08-26 — 6 − 1 = 5) — see `CLIENT_REQUIREMENTS_GAPS.md` for the full list. GAP-001/GAP-002/GAP-003/GAP-004/GAP-005/GAP-006/GAP-007/GAP-027 are all RESOLVED. ~~GAP-022 (`Student.school`) is a NEW OPEN HIGH finding~~ **GAP-022 RESOLVED 2026-08-25 — was Round 2's own audit error, not a code defect.**

**Revision note (2026-08-24):** these totals were revised from the audit's first draft (126 requirements, 11 INCORRECT, 17 PARTIAL, 2 NOT_TESTABLE) after cross-checking this report against three independently-run evidence passes on backend RBAC, database schema, and frontend pages, which surfaced REQ-RBAC-012 (new row, INCORRECT — GAP-020) and resolved REQ-DEPT-002 from NOT_TESTABLE to PARTIAL/MISSING(FE) (GAP-021). Neither changed the FINAL CUSTOMER ACCEPTANCE decision at that point, since both were MEDIUM severity, not CRITICAL/HIGH.

**Remediation update (2026-08-25):** see `docs/requirements/CLIENT_ACCEPTANCE_REMEDIATION_REPORT.md` for the full Blocker Fix Phase report. All 2 CRITICAL and 4 of 5 HIGH findings (GAP-001 through GAP-005, GAP-007) were reported REMEDIATED with real code, migrations, and passing automated tests. GAP-006 was reported PARTIALLY remediated.

**RE-AUDIT ROUND 2 update (2026-08-25):** see `docs/requirements/CLIENT_ACCEPTANCE_REAUDIT_ROUND2.md` for the full independent re-verification. Round 2 did **not** accept the remediation report's own self-assessment at face value — every blocker was re-derived from the Excel wording and re-read against current code. Result: **3 of 6 blockers are genuinely fully closed** (Export Control, Payment-Gated Activation, Task.output — RESOLVED). **3 of 6 blockers were downgraded from "Remediated" to "Partially Remediated"** after finding real, concrete uncovered pieces of the original requirement: Student Mandatory Fields (Round 2 believed the `school`/"Trường-Lớp" field was never implemented at all — this specific sub-claim was itself corrected on 2026-08-25, see below; the other Student-field gaps in this same item stand unchanged), Closure/Liquidation (2 of 3 named completion obligations — "dịch vụ hoàn thành," "tài liệu bàn giao" — are not checked, and liquidation's "xác nhận hai bên" is unenforced free text), and Commission Traceability (Visa leg remains unimplemented, and Round 2 determined the Excel gives no basis to treat it as lower-priority than the Contract/Scholarship legs that were fixed). One further new finding not previously caught: REQ-RBAC-013 — HCTH/ADMIN_FINANCE has zero access to Student Profile despite sheet03 specifying "Hạn chế" (restricted), the 4th confirmed instance of the same under-provisioning pattern. One new formal conflict: CONFLICT-004 (the customer's own sheet04 and sheet17 disagree on whether Student.gpa is required). None of the downgrades are regressions — every partial fix that Round 1 built is real, tested, and a genuine improvement — but none should be represented to the client as fully closed.

**Post-Round-2 correction (2026-08-25):** Round 2's own `school`/"Trường-Lớp" finding above (originally GAP-022/REQ-STUDENT-007, "never implemented at all") was itself an audit error. `AcademicRecord.school String` (NOT NULL) has existed all along, required at the DTO layer and the UI form layer — Round 2's search only checked `model Student` in the schema and never checked `model AcademicRecord`, where `grade`/`gpa` (which Round 2 DID credit) also live. This is now GAP-022 RESOLVED, not an open HIGH finding — see `CLIENT_ACCEPTANCE_MATRIX.md`'s "POST-ROUND-2 CORRECTION" section for the full evidence. No code changed; this is a correction to the audit's own prior output. One genuinely new, minor, non-mandatory finding surfaced during the same check: GAP-026 (`Activity` has no `award` field, unlike `Competition`/`ResearchProject`) — tracked as new row REQ-STUDENT-008.

**Client decisions applied (2026-08-25):** The client then directly answered three open questions from `CLIENT_CLARIFICATION_SIGNOFF.md`. (1) Gating Student mandatory fields at Assessment-approval time, not creation time, is **confirmed as the intended design** — this was the only thing keeping GAP-004/GAP-005 and REQ-STUDENT-002/004/007 at PARTIAL, so GAP-004 and GAP-005 are now RESOLVED (REQ-STUDENT-004 upgraded to IMPLEMENTED). (2) CONFLICT-004 (GPA required vs. optional) is resolved: **GPA is Optional**. This decision surfaced a genuine, previously-unflagged code mismatch — `assertStudentProfileComplete` still required GPA at the Assessment-approval gate — tracked as **GAP-027** (MEDIUM). (3) Awards for pure extracurricular activities (GAP-026/REQ-STUDENT-008) — **confirmed the field should be added.**

**Code fixes applied (2026-08-25):** The client then asked for GAP-027 and GAP-026 to be fixed in code, not just recorded. **GAP-027**: `assertStudentProfileComplete` no longer requires `gpa`, only `grade` — REQ-STUDENT-002 upgraded PARTIAL → IMPLEMENTED. **GAP-026**: `Activity.award String?` added (migration `20260825084819_activity_award_field`), wired through backend DTO and frontend types/form/display — REQ-STUDENT-008 upgraded PARTIAL → IMPLEMENTED. A full re-check of every one of sheet04's 18 field rows against current code, done as part of this same pass, found no further Student Profile gaps; it did surface and fix two stale UI-copy references to "GPA required" that had never affected actual enforcement (a hint string and an error message). Backend/frontend `tsc --noEmit` clean, `eslint` clean, and `assessment-roadmap.e2e-spec.ts` (20/20), `profile-evidence.e2e-spec.ts` (17/17), and the frontend profile page test (5/5) all re-verified passing. See `CLIENT_ACCEPTANCE_MATRIX.md`'s "CLIENT DECISIONS APPLIED" and "CODE FIXES APPLIED (2026-08-25)" sections for full detail.

## CRITICAL FINDINGS: 0 (both independently re-confirmed RESOLVED in Round 2)
1. ~~**GAP-001** — Export Control not implemented~~ — RESOLVED: `EXPORT_ROW_CAP=5000` enforced on all 4 export endpoints, 409 on overflow, field-redaction confirmed not bypassed. See ASM-87. (Round 2 found one cosmetic doc-citation bug: the code comment cites the wrong ASM number — zero functional impact.)
2. ~~**GAP-002** — Contract could activate with zero payment recorded~~ — RESOLVED: SIGNED→ACTIVE now requires a received payment, race-safe (concurrency e2e test re-read line-by-line in Round 2). See ASM-88; exact threshold remains registered as CONFLICT-001, correctly not silently resolved.

## HIGH FINDINGS: ~~1 remaining~~ **0 remaining (2026-08-27 — GAP-006 resolved via DEC-09, the last HIGH finding)**
~~GAP-003~~ (Task.output — RESOLVED, no bypass found, ASM-89). ~~**GAP-004/GAP-005**~~ **GAP-004/GAP-005 — RESOLVED 2026-08-25** (Student fields — all 8 named fields now solid: `school` was confirmed to already exist on `AcademicRecord` (GAP-022 was a Round 2 audit error, corrected 2026-08-25, not a code gap); the stage-timing question that kept these PARTIAL is now client-confirmed as intended design. The narrow residual — code still requiring GPA at the gate, after the client separately decided GPA is Optional — is tracked as new finding GAP-027, MEDIUM, not part of this HIGH finding.) ~~**GAP-007**~~ **GAP-007 — RESOLVED 2026-08-26** (Closure/Liquidation — the client answered DEC-06/07/08 and the unified Closure workflow was implemented: HCTH standard executor / ED-DM audited override / Consultant request-only; all 6 mandatory preconditions including a real document-handover check; a structured, immutable two-party liquidation confirmation. See `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md`.) ~~**GAP-006** (Contract↔Partner commission link — PARTIALLY REMEDIATED, the sole remaining HIGH item...)~~ **GAP-006 — RESOLVED 2026-08-27 (DEC-09)** — client confirmed the Visa leg is mandatory for Visa-sourced commissions specifically ("bắt buộc theo loại nguồn hoa hồng"), not mandatory otherwise. `CommissionTransaction.visaId` built, auto-resolved by the same mechanism already proven for `contractId`; a Visa-sourced transaction's rule must be FIXED-basis (Visa carries no monetary field), enforced explicitly. 5 new e2e tests, full regression green. **This closes every HIGH finding from the original audit.**

## MEDIUM FINDINGS: 13 currently open (13 original + 3 new found in the 2026-08-27 sheet-by-sheet re-check, minus 3 resolved: GAP-027, GAP-011, GAP-024)
GAP-008/GAP-009 (RBAC under/over-provisioning on Partner Documents and Visa for two roles), GAP-010 (Partner type enum mismatch), GAP-012 (missing Contract.contract_type field), GAP-013 (unimplemented Training entity), GAP-014 (unresolved at-rest-encryption scope question). Plus GAP-020 (Sale/Marketing under-provisioned on Student Profile/Competition) and GAP-021 (Marketing has no standalone frontend module). **GAP-023 (Round 2)** — HCTH/ADMIN_FINANCE zero access to Student Profile, the 4th confirmed instance of the same "Hạn chế→zero" pattern (now spanning 3 roles × 4 module cells — worth a full seed.ts-vs-sheet03 audit, not more one-off patches). **GAP-025 (Round 2)** — root cause under GAP-020: the `profile_evidence` RBAC resource is too coarse to express the Excel's per-module Sale/Marketing distinction even if the missing grant were added. ~~GAP-027~~ **RESOLVED 2026-08-25** (GPA gate). ~~GAP-011~~ **RESOLVED 2026-08-27** (Partner Program ID prefix — `PP-CC-NNNNN-NN` now correct, 5 new e2e tests, backend e2e 544/544). ~~GAP-024~~ **RESOLVED 2026-08-26** (`Case.stage` — now a controlled 15-value enum, DEC-14). **NEW 2026-08-27 (final sheet-by-sheet pass, sheets 10-20):** **GAP-028** — Partner has no pre-onboarding "Prospect" status (only ACTIVE/INACTIVE). **GAP-029** — Contract has no direct FK to PartnerProgram (sheet19 row14, same family as GAP-006). **GAP-030** — Contract Templates: DB query confirms 0 rows exist (mechanism works, data never seeded); also no field for sheet12's audience/scope/has-appendix columns.

## LOW FINDINGS: 6 currently open (7 total identified — GAP-026 resolved 2026-08-25)
GAP-015 through GAP-019 plus the `.env`-points-at-production operational-hygiene note (already corrected during remediation). ~~GAP-026 — `Activity` model has no `award` field~~ **GAP-026 RESOLVED 2026-08-25 — `Activity.award` added.** None of the still-open items are individually blocking.

---

## END-TO-END JOURNEYS (9 scenarios — Round 2 re-derived fresh from code, full detail in `CLIENT_ACCEPTANCE_MATRIX.md`'s "Customer Journey Scenarios" section)

| Scenario | Round 2 result |
|---|---|
| A — Lead→Contract→Student→Case | PARTIAL (sequencing differs from customer's literal description — signing links an already-existing Student/Case rather than originating them) |
| B — Case→Assessment→Roadmap→Profile→Writing | IMPLEMENTED (best-evidenced gate in the system) |
| C — Case→University Choice→Application→Checklist→Offer | IMPLEMENTED (entities), sequencing not system-enforced (Excel doesn't require it either) |
| D — Application→Scholarship→Result | PARTIAL (thin result-reporting UX, unchanged) |
| E — Visa→Pre-departure→Enrollment | **IMPLEMENTED (Round 2: Pre-departure's backing model now conclusively resolved)** — shared `VisaChecklistItem` model, `entityType:'PreDeparture'`, a deliberate documented reuse pattern, not a missing entity |
| F — Contract→Payment→Closure→Archive | **IMPLEMENTED (upgraded 2026-08-26, after Round 2's downgrade from Round 1's "IMPLEMENTED")** — payment-gated activation and Archive are both solid and confirmed walkable end-to-end. Closure's former residual gaps (document handover unchecked, HCTH unreachable service-completion path, unenforced liquidation confirmation) are now resolved by the unified Closure workflow (DEC-06/07/08) — see `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md`. |
| G — Partner→Program→Student Link→Commission | ~~PARTIAL (functionally complete, Visa-leg traceability gap remains)~~ **IMPLEMENTED (2026-08-27) — Visa leg built via DEC-09; all three traceability legs (Contract/Scholarship/Visa) now real.** |
| H — Student Portal full journey | IMPLEMENTED (strongest evidence of any scenario) |
| I — Parent Portal linked-child | **IMPLEMENTED (Round 2: independently re-verified, not carried forward unchecked)** — `GET /portal/me` genuinely server-resolves every linked student via the real `StudentContact` table; a parent with >1 child sees a real picker UI. Round 1 had asserted this without opening the code. |

## ROLE UAT
See `CLIENT_UAT_SCENARIOS.md` role table. All 8 roles have purpose-built demo fixtures already seeded in the codebase (`database/seeds/seed.ts`) specifically for this kind of test, but none were exercised live this pass. Permission-boundary confidence is HIGH (extensively cross-checked against actual seed grants); lived-experience confidence is LOW (nothing was watched render).

## SECURITY CONFORMANCE
Strong overall — genuinely exceeds the customer's baseline in several areas (MFA fully wired though only recommended, session control with live per-request re-validation, immediate offboarding revocation, non-enumerating 404-based document/record access, defense-in-depth field redaction layered on top of scope filtering). **Export Control is now remediated** (row-capped, 409 on overflow) — the encryption-at-rest scope question (GAP-014, MEDIUM) is the remaining item keeping this from a clean pass. RBAC role→module mapping is faithful for the highest-risk cells (Sale/Marketing, HCTH) with a handful of narrower under/over-provisioning mismatches (GAP-008/009, untouched by this remediation phase).

## DATA MODEL CONFORMANCE
Still the weakest area overall, but improved. `Student.scholarshipGoal`, `AcademicRecord.grade`, `Contract.closureReason`, `CommissionTransaction.contractId`, `PartnerStudentLink.contractId`/`scholarshipApplicationId`, and (2026-08-27) `CommissionTransaction.visaId` were all added (additive migrations, no data loss), and the customer-mandatory Student/Task fields now have real, tested enforcement (stage-aware for Student, at the DONE transition for Task) even though the underlying DB columns remain nullable by design. Several sheet-19 relationships are still not implemented as direct FKs (Application/Scholarship→PartnerProgram, Student→Document/Task, `PartnerStudentLink`→Visa — the latter deliberately out of scope per DEC-09's own wording, not an oversight), and Partner's type taxonomy still doesn't match the customer's 7 categories. None of these break the *running* system — the app clearly works around them via alternate paths.

## ID/FORMAT CONFORMANCE
~~13 of 14 customer ID prefixes are implemented correctly with real DB-level uniqueness and confirmed immutability (no update endpoint accepts a code override). Two defects: Partner Program's generated ID carries the wrong prefix (`PT-` instead of `PP-`), and Training (TRN) has no implementation at all.~~ **Updated 2026-08-27:** 14 of 14 customer ID prefixes are now implemented correctly except Training. The Partner Program prefix defect (GAP-011) is RESOLVED — `nextPartnerProgramSuffix` now emits `PP-CC-NNNNN-NN` — with real DB-level uniqueness and confirmed immutability. **Training (TRN) remains intentionally unimplemented** — the Training domain does not currently exist and requires a separate scope decision from the client; this was explicitly out of scope for the identifier-prefix fix. No ID-based-authorization violations found (Student/Contract both scope-check before every fetch), with one architectural inconsistency on Partner reads worth a deliberate decision (REQ-RBAC-011, unchanged — Partner record-level authorization was explicitly out of scope for this fix).

## AUDIT CONFORMANCE
Real, append-only, well-structured audit trail covering all 8 customer-required columns, with genuine VIEW/DOWNLOAD/EXPORT capture including denial outcomes. **Export volume is now capped, not just logged** (Remediated GAP-001). Remaining gap: list/browse reads still aren't audited (only single-record views, LOW severity, untouched).

## KPI/SLA CONFORMANCE
The core, security-relevant KPIs (on-time completion rate, overdue-task count, quality score, org-wide pipeline/financial dashboards) are genuinely computed server-side with correct multi-currency handling. The softer/newer KPIs (case-count-per-staff, writing-artifact completion counts, on-time-closure rate) are missing from the reporting layer, and two (internal-profile-error count, customer-response SLA) have no underlying data model at all — these need a scoping conversation with the client before they can be built, since the customer sheet doesn't define what starts/stops the clock or what counts as an "internal error."

## FRONTEND CONFORMANCE
Strong, and now stronger. Every customer module has a real page or a reasonable tab-consolidated equivalent — **including Closure/Liquidation**, which now has a full unified workflow page (`/cases/[id]/closure`) with a live 6-item checklist, document handover, and two-party liquidation confirmation (staff + Portal), not just a debt-check page (Resolved GAP-007, 2026-08-26). Role-based navigation, empty/loading/error/forbidden states, Vietnamese labeling, and the full Student/Parent portal are all confirmed consistently implemented, not spot exceptions. Dashboards are genuinely server-computed, not client-side aggregations. One maintenance-risk note: the frontend nav permission table is a hand-authored mirror of the backend RBAC seed, not fetched live — low security risk (backend re-enforces everything) but a real drift risk over time.

## BACKEND CONFORMANCE
Strong, and now stronger. Every workflow-stage entity and status machine was traced with exact file:line citations. Roadmap-approval gating, Contract's SIGNED-immutability guard, and the new payment-gated activation/debt-checked completion transitions (interactive transaction + compare-and-swap, race-tested) are the best-evidenced, most rigorously enforced business rules in the codebase. **Both CRITICAL gaps (unbounded export, ungated contract activation) are now fixed** — narrow, well-understood backend logic changes, exactly as previously assessed, verified by new automated tests exercising both the failure and success paths.

## DATABASE CONFORMANCE
Schema is well-normalized and the migration history is clean (20 migrations, confirmed up to date against production in this audit's own tooling check). The Data Dictionary conformance gaps (missing/optional required fields, a few non-FK "relationships") are real but narrow — most of the underlying business need is still met through an alternate, arguably more normalized path (e.g., GPA history via `AcademicRecord` rather than a single `Student.gpa` field).

---

## FINAL CUSTOMER ACCEPTANCE: ~~PASS WITH CONDITIONS (re-confirmed, 2026-08-26 — 1 HIGH condition remaining)~~ **PASS WITH CONDITIONS (re-confirmed, 2026-08-27 — 0 CRITICAL, 0 HIGH remaining; conditions are now MEDIUM/LOW backlog + 2 open formal CONFLICTs needing client sign-off only)**

**Original reasoning (2026-08-24, superseded):** *"FAIL: Any mandatory CRITICAL/HIGH requirement missing/incorrect."* Two CRITICAL findings and five HIGH findings sat against explicitly "Bắt buộc" customer requirements.

**Remediation-time reasoning (2026-08-25, superseded by Round 2):** All 2 CRITICAL and 4 of 5 HIGH findings were reported REMEDIATED; 1 HIGH (GAP-006) reported PARTIALLY remediated. **This framing was not independently re-verified before being reported — Round 2 exists specifically to check it.**

**Re-Audit Round 2 reasoning (2026-08-25, superseded in part by the correction below):** Independently re-deriving every blocker's status from the Excel wording (not accepting the remediation report's own self-grading) found the Round-1 picture was too optimistic in 3 of 6 places. The corrected picture: **0 CRITICAL remain** (both independently re-confirmed RESOLVED — Export Control and Payment-Gated Activation are both real, tested, and race-safe). Round 2 reported **4 HIGH remaining** (down from the original 5, up from Round 1's reported 1): Task.output is genuinely RESOLVED; Student Fields, Closure/Liquidation, and Commission Traceability are each PARTIALLY remediated with a concrete, named, uncovered piece of the original mandatory requirement (see the HIGH FINDINGS section above); plus one reported wholly new HIGH finding (GAP-022, `Student.school`) the original remediation was said to have missed entirely.

**Post-Round-2 correction (2026-08-25):** GAP-022 above was itself a Round-2 audit error, not a code defect — `AcademicRecord.school` (NOT NULL, DTO- and form-required) has existed all along; Round 2's search only checked `model Student`, never `model AcademicRecord`. Correcting for this alone: **3 HIGH remain**, not 4 — Task.output is RESOLVED; Student Fields, Closure/Liquidation, and Commission Traceability remain PARTIALLY remediated on their own original, unrelated merits (unaffected by this correction).

**Client decisions applied (2026-08-25, current):** The client then directly confirmed the stage-timing question underlying the "Student Fields" HIGH finding — gating at Assessment-approval time, not creation time, is the intended design. That was the only thing keeping GAP-004/GAP-005 PARTIAL, so **Student Fields is now RESOLVED, bringing HIGH down to 2 remaining**: Closure/Liquidation (GAP-007) and Commission Traceability (GAP-006). The client separately resolved CONFLICT-004 (GPA required vs. optional) as Optional, which exposed a new, narrower MEDIUM finding (GAP-027 — code not yet updated to match) rather than reopening a HIGH one. Per this audit's own decision rules — *"PASS WITH CONDITIONS: no CRITICAL/HIGH **mandatory failure**, remaining gaps are non-critical, each has explicit explanation"* — the 2 remaining HIGH items are not full failures (each has a real, working partial fix and a precisely-scoped remaining piece, all explicitly documented here and in the matrix) — the system holds at **PASS WITH CONDITIONS**, not FAIL. The *conditions* list is now two items shorter than Round 2 originally reported (no `Student.school` fix needed, and Student Fields' stage-timing question is closed), but neither of the 2 remaining downgraded items (GAP-006/GAP-007) should be represented to the client as fully closed.

**Closure/Liquidation remediation applied (2026-08-26, current):** The client answered DEC-06/07/08 (`CLIENT_CLARIFICATION_SIGNOFF.md`) and the unified Closure workflow was implemented — see `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md` for the full design and verification record. **GAP-007 is now RESOLVED, bringing HIGH down to 1 remaining: Commission Traceability (GAP-006)** — unrelated to Closure, explicitly out of scope for this remediation. The system holds at **PASS WITH CONDITIONS**, one condition shorter than the previous framing; GAP-006 should not be represented to the client as closed.

**Commission↔Visa traceability resolved (2026-08-27, current):** The client answered DEC-09 (`CLIENT_CLARIFICATION_SIGNOFF.md` §10) directly — the Visa leg is mandatory for Visa-sourced commissions, not for others; `visaId` nullable at the data level, required per commission source type. `CommissionTransaction.visaId` was built, auto-resolved by `resolveSource()` — the exact same mechanism already proven for `contractId` — with an explicit guard rejecting a non-FIXED rule on a Visa source (Visa has no monetary field to compute a percentage from). 5 new e2e tests; ~~full regression suite green (backend unit + e2e, frontend).~~ **backend unit 209/209, backend e2e 556/557, frontend 325/325 (2026-08-27 re-run). The single e2e failure (`rbac.e2e-spec.ts`'s case-member-scoped student-list test) was traced directly against the DB and is a pre-existing, unrelated flake — 106+ case memberships accumulated on a fixture user across this session's repeated e2e runs against the never-reset persistent dev DB, pushing a fixture student outside a default 100-row page window; nothing to do with Commission or Visa, not modified. Also live-browser-verified end-to-end the same day: a real FIXED-basis commission rule and a real Visa-sourced transaction were created through the actual UI, run through eligibility-confirm → calculate, and the result cross-checked directly in the DB (test data cleaned up after). See `docs/DECISIONS.md` DEC-16's verification note for the full detail.** **GAP-006 is now RESOLVED — this was the last remaining HIGH finding from the entire audit. HIGH FINDINGS: 0. CRITICAL FINDINGS: 0.**

Per this report's own decision rule (line 111 above): *"FAIL: Any mandatory CRITICAL/HIGH requirement missing/incorrect."* With zero CRITICAL and zero HIGH findings remaining, there is no basis for FAIL. However, this report's own "REMAINING ITEMS" section (below) frames a **clean PASS** — one step beyond "PASS WITH CONDITIONS" — as requiring every remaining item to be *"resolved or [have] explicit client acceptance,"* not just CRITICAL/HIGH ones. **13 MEDIUM findings, 6 LOW findings, and 2 open formal CONFLICT items (CONFLICT-002/SYSTEM_ADMIN in full, CONFLICT-001's literal PAYMENT/AMENDED-as-enum-value sub-question) are still open.** The system therefore holds at **PASS WITH CONDITIONS**, not a clean PASS — but every remaining condition is now non-critical by this audit's own severity taxonomy (MEDIUM/LOW backlog items, plus 2 conflicts that need a client decision, not a code defect). This is a materially different — and materially better — position than any prior framing of this verdict, and should be represented to the client as such: **no functional blocker remains; what's left is backlog triage and two decisions.**

This is a genuine, independently re-verified assessment — every one of the 6 blockers was re-read against the Excel and the current code a second time, by a process explicitly instructed not to trust the first pass's self-assessment — and the re-verification process itself was subsequently checked a third time against a fuller sheet04 read, which is what surfaced and corrected the GAP-022 error above. See `docs/requirements/CLIENT_ACCEPTANCE_REAUDIT_ROUND2.md` for the full Round 2 reasoning and re-derived citations (that document's own GAP-022 narrative has not been rewritten in place — see its top-of-file correction note instead, per this project's "never silently rewrite" convention).

## REMAINING ITEMS (must resolve or obtain explicit client acceptance before a re-audit could reach a clean PASS)
1. ~~Export row-cap~~ — DONE, re-confirmed Round 2. Confirm the 5000-row threshold with the client (not customer-specified).
2. ~~Payment-gated activation~~ — DONE, re-confirmed Round 2. ~~Confirm the exact payment threshold with the client (CONFLICT-001 — "any payment" vs. a specific amount).~~ **RESOLVED 2026-08-27 (DEC-01): client set the threshold at 30% of Contract.value, net of refunds. Implemented (`ACTIVATION_PAYMENT_THRESHOLD_RATIO`), tested. CONFLICT-001's narrower literal-modeling sub-question (PAYMENT/AMENDED as `ContractStatus` enum values) remains open — see item 8.**
3. ~~Task.output enforcement~~ — DONE, re-confirmed Round 2, no bypass found.
4. ~~**Student required fields** — 8 of 8 named fields DONE (stage-aware, at Assessment approval; confirm this stage choice with the client).~~ **RESOLVED 2026-08-25: the client confirmed the stage-timing choice is intended, and confirmed GPA is Optional (not one of the 8 required fields anymore). `school` already exists on `AcademicRecord` (GAP-022 was a Round 2 audit error, corrected). GAP-027's code follow-up (drop `gpa` from the Assessment-approval gate) is done — nothing left for this item.**
5. ~~**Closure/Liquidation UI** — the debt-check half is DONE and reachable through a real page. Add a service-completion precondition and a document-handover check (2 of 3 named COMPLETED obligations are still unchecked); add a real two-party confirmation mechanism to LIQUIDATED (currently free text).~~ **RESOLVED 2026-08-26 — the client answered DEC-06/07/08 and the unified Closure workflow was implemented (all 6 preconditions including document handover; structured two-party liquidation confirmation; HCTH standard/ED-DM override/Consultant request-only). See `docs/requirements/CLOSURE_LIQUIDATION_DESIGN.md`. Nothing left for this item.**
6. ~~Add `PartnerStudentLink.visaId` / `CommissionTransaction.visaId` to fully close GAP-006 — Round 2 determined this should be treated as an unresolved mandatory gap, not a minor footnote.~~ **RESOLVED 2026-08-27 (DEC-09): `CommissionTransaction.visaId` built and tested — this is what the client asked for. `PartnerStudentLink.visaId` was deliberately not asked for and stays unbuilt — a separate, still-open item if the client wants it, not implied by DEC-09.**
7. **NEW (Round 2):** Add a Student Profile grant (at least restricted/view) for HCTH/ADMIN_FINANCE (GAP-023/REQ-RBAC-013) — 4th confirmed instance of the same under-provisioning pattern; consider a full seed.ts-vs-sheet03 cell-by-cell audit rather than more one-off patches.
8. Resolve the four formal CONFLICT items (contract status sequence, SYSTEM_ADMIN role sign-off, Partner data-access-scope reading, ~~and the new Student.gpa sheet04-vs-sheet17 contradiction~~ **CONFLICT-004/GPA resolved 2026-08-25, code follow-up (GAP-027) done**) with the client directly — ~~3 of 4 remain open~~ ~~2026-08-27: CONFLICT-001's payment-threshold sub-question is resolved (DEC-01, 30%); its literal PAYMENT/AMENDED-as-enum-value sub-question, plus CONFLICT-002 (SYSTEM_ADMIN) and CONFLICT-003 (Partner data-access scope) in full, remain open — 2 of 4 fully open, 1 partially resolved, 1 fully resolved.~~ **2026-08-27 (later same day): CONFLICT-003 (DEC-03) is now also fully resolved — Partner is client-confirmed company-wide shared data, no case-scoping needed, no code change required. Standing: 2 of 4 fully resolved (CONFLICT-003, CONFLICT-004), 1 partially resolved (CONFLICT-001 — threshold done, literal-enum question open), 1 fully open (CONFLICT-002, SYSTEM_ADMIN).**
9. MEDIUM/LOW items (GAP-008 through GAP-021, GAP-023 through GAP-025) — batch into a normal backlog pass; none are release-blocking on their own. ~~GAP-026~~/~~GAP-027~~ done, no longer part of this backlog.

## RECOMMENDED FIX ORDER (updated, 2026-08-25 code fixes applied)
1. ~~Confirm the open thresholds/stage questions with the client (export row cap, payment-activation amount, Student-field enforcement stage including `school`, GPA requiredness) — no code changes needed until answered.~~ **Stage-timing and GPA requiredness are now confirmed (2026-08-25), and the GPA code follow-up is done (item 2 below). Export row cap and payment-activation amount remain open.**
2. ~~Add `Student.school` (GAP-022) — small, same pattern as `scholarshipGoal`, and it's the one genuinely missing piece of an otherwise-solid fix.~~ **Removed 2026-08-25: `school` already exists on `AcademicRecord`. No code change needed — GAP-022 RESOLVED (was a Round 2 audit error).** ~~Update `assertStudentProfileComplete` to drop the `gpa: { not: null }` clause~~ **DONE 2026-08-25 — GAP-027 fixed.** ~~Add `Activity.award`~~ **DONE 2026-08-25 — GAP-026 fixed.**
3. ~~Add `PartnerStudentLink.visaId`/`CommissionTransaction.visaId` (GAP-006 residual)~~ **DONE 2026-08-27 — `CommissionTransaction.visaId` built (DEC-09); `PartnerStudentLink.visaId` deliberately out of scope, not asked for.** And a Student Profile grant for HCTH (GAP-023) — still open.
4. ~~Add the service-completion and document-handover checks to Contract COMPLETED, and a real two-party confirmation mechanism to LIQUIDATED (GAP-007 residual) — the larger remaining item, needs a design decision on which role/data performs the service-completion check given ADMIN_FINANCE's lack of `cases:view`.~~ **DONE 2026-08-26 — client answered DEC-06/07/08, unified Closure workflow implemented.**
5. ~~Resolve the remaining 3 formal CONFLICT items with the client (CONFLICT-004 is done).~~ **2026-08-27: CONFLICT-001 (threshold sub-question) and CONFLICT-003 resolved. Only CONFLICT-002 (SYSTEM_ADMIN) and CONFLICT-001's literal-enum-value sub-question remain.**
6. MEDIUM/LOW items — normal backlog pass.
7. Execute the 9 UAT scenarios and role matrix live in a browser against the existing `demo.*` seed fixtures — the automated e2e suite proves the API-level behavior genuinely works, but no one has watched the actual UI render through a full user journey yet. **Partial progress (2026-08-27):** this is not yet the full formal sweep, but targeted live-browser spot-checks have since been run for several specific fixes, each with a real UI action, a real result, and a direct DB cross-check: the Audit Log page, the `Case.stage` 15-value dropdown, the Partner Program ID prefix fix (GAP-011), DEC-05(b) School Master (full pass — see `CLIENT_ACCEPTANCE_MATRIX.md` REQ-STUDENT-007), and DEC-09 Commission↔Visa (full pass — see `docs/DECISIONS.md` DEC-16). **Not yet live-verified:** the DEC-01 payment-activation threshold (30%) is e2e-tested only, not watched live in a browser. The full 9-scenario sweep and the full 8-role dashboard/module/export walk (see `CLIENT_UAT_SCENARIOS.md`) remain outstanding.

---

Do NOT deploy based on this report alone. Do NOT start a new feature phase from these findings without client conversation on the CONFLICT items. No production data was read, modified, or fabricated in the course of this audit or its remediation. No customer Excel file was modified. See `docs/requirements/CLIENT_ACCEPTANCE_REMEDIATION_REPORT.md` for the full Blocker Fix Phase report.
